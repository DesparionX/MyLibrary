<Window
    x:Class="MyLibrary.Views.AddItemToReceiptWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MyLibrary.Views"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:resx="clr-namespace:MyLibrary.Resources.Languages"
    xmlns:viewmodels="clr-namespace:MyLibrary.ViewModels"
    x:Name="NewItemToReceipt"
    Title="AddItemToReceiptWindow"
    d:DataContext="{d:DesignInstance Type=viewmodels:AddItemToReceiptViewModel}"
    AllowsTransparency="True"
    Background="#70000000"
    FontFamily="Jura Bold"
    ResizeMode="NoResize"
    Style="{DynamicResource MaterialDesignWindow}"
    Topmost="True"
    WindowStartupLocation="CenterScreen"
    WindowState="Maximized"
    WindowStyle="None"
    mc:Ignorable="d">
    <Grid>
        <Border
            Width="400"
            MinHeight="220"
            Padding="5"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Background="{DynamicResource MaterialDesign.Brush.Primary.Dark}"
            ClipToBounds="True"
            CornerRadius="30">
            <Border
                Padding="0"
                Background="{DynamicResource MaterialDesignPaper}"
                CornerRadius="30">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50" />
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                    </Grid.RowDefinitions>
                    <Button
                        Grid.Row="0"
                        Width="50"
                        Height="50"
                        Margin="0,0,10,0"
                        Padding="0"
                        HorizontalAlignment="Right"
                        HorizontalContentAlignment="Center"
                        VerticalContentAlignment="Center"
                        Background="Transparent"
                        BorderThickness="0"
                        Click="CancelButton_Click"
                        Foreground="{DynamicResource MaterialDesign.Brush.Primary.Dark}"
                        RenderTransformOrigin="0.5,0.5"
                        Style="{StaticResource CloseButton}">
                        <Button.RenderTransform>
                            <TransformGroup>
                                <ScaleTransform ScaleX="1.5" ScaleY="1.5" />
                                <SkewTransform />
                                <RotateTransform />
                                <TranslateTransform />
                            </TransformGroup>
                        </Button.RenderTransform>
                    </Button>
                    <TextBox
                        x:Name="ISBN"
                        Grid.Row="1"
                        Grid.Column="0"
                        Width="300"
                        Height="50"
                        VerticalAlignment="Center"
                        materialDesign:HintAssist.Hint="{x:Static resx:Strings.Recipe_Header_ISBN}"
                        Focusable="True"
                        LostFocus="Validate_ISBN"
                        Style="{DynamicResource MaterialDesignOutlinedTextBox}"
                        TextAlignment="Center"
                        TextChanged="Validate_ISBN"
                        TextWrapping="Wrap" />
                    <TextBlock
                        x:Name="ISBNValidator"
                        Grid.Row="2"
                        Margin="0,10,0,0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Background="Transparent"
                        Foreground="Red"
                        Text="{Binding ReceiptErrors}"
                        TextAlignment="Center"
                        TextWrapping="Wrap"
                        Visibility="{Binding ErrorsVisibility}" />
                    <Button
                        Grid.Row="3"
                        Grid.Column="0"
                        Width="100"
                        Height="50"
                        Margin="0,10"
                        Padding="0"
                        VerticalAlignment="Bottom"
                        materialDesign:ButtonAssist.CornerRadius="10"
                        Click="ConfirmButton_Click"
                        Content="Add"
                        FontSize="20"
                        Style="{DynamicResource LogInButton}" />
                </Grid>
            </Border>
        </Border>
    </Grid>
</Window>
